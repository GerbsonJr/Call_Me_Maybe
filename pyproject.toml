[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function calling with a small LLM"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.0",
    "pydantic>=2.0",
    "llm-sdk",
]

[dependency-groups]
dev = [
    "flake8>=7.0",
    "mypy>=1.0",
]

[tool.mypy]
exclude = [
    "^\\.venv/",
    "^llm_sdk/"
]

[tool.uv.sources]
llm-sdk = { path = "llm_sdk" }
