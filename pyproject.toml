[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function calling with a small LLM"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pydantic",
    "llm-sdk",
]

[tool.uv.sources]
llm-sdk = { path = "llm_sdk" }
